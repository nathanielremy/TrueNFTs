name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-west-3

    - name: Deploy static site to S3 bucket
<<<<<<< HEAD:.github/workflows/main.yml
      run: aws s3 sync ./TrueNFTs/Public s3://www.truenfts.com --delete
=======
      run: aws s3 sync ./TrueNFTs/Public/ s3://www.truenfts.io --delete
>>>>>>> 473f5fa3f5e4b02623ad0684764033a998c20bdf:.github/workflows/startup.yml
